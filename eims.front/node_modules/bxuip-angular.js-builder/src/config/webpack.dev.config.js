/**
 * Created by UI/UX Team on 2018. 2. 14..
 */

const path = require('path');
const bxuipBuilderCore = require('bxuip-builder-core');

const {builderPath: coreBuilderPath} = bxuipBuilderCore;
const {Config: WebpackConfig, environment} = require('webpack-config');

const coreBuilderConfigPath = path.join(coreBuilderPath, 'src/config/webpack.dev.config.js');

const config = {};

module.exports = new WebpackConfig().extend(
    path.join(__dirname, 'webpack.base.config.js'),
    coreBuilderConfigPath
).merge(config);