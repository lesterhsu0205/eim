/**
 * Created by UI/UX Team on 2018. 2. 14..
 */

const _ = require('lodash');

const bxuipBuilderCore = require('bxuip-builder-core');

const Config = new bxuipBuilderCore.Config;

// 코어 빌더와 프로젝트 빌더 설정을 병합한다.
Config.merge(require('../../config'));

const {Config: WebpackConfig, environment} = require('webpack-config');

const config = {};

module.exports = new WebpackConfig().merge(config);